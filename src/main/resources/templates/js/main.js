// Inicialización del calendario de reservas (movido desde index.html)
// Aseguramos que el DOM esté listo antes de buscar elementos
document.addEventListener('DOMContentLoaded', () => {
	// La lógica de autenticación y cabecera se ha movido a Thymeleaf (lado del servidor)
    // para evitar parpadeos y problemas de carga.
    // Ver: fragments/header.html
});

